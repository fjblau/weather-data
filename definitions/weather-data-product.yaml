product: '1.0.0'

info:
  name: weather-data
  title: weather-data
  version: '1.0.0'

apis:
  'weather-data':
    $ref: weather-data.yaml

visibility:
  view:
    type: public
  subscribe:
    type: authenticated

plans:
  default:
    title: Default Plan
    description: Default Plan
    approval: false
    rate-limit:
      value: 100/hour
      hard-limit: false
